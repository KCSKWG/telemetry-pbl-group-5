import pandas as pd
from pathlib import Path

# extracted CSV/XLSX file path here
RAW_FILE = "raw_data.csv"
OUTPUT_FILE = "water_quality_data.csv"


def standardize_columns(df: pd.DataFrame) -> pd.DataFrame:
    rename_map = {}

    for c in df.columns:
        c_low = c.strip().lower()

        if "time" in c_low or "date" in c_low:
            rename_map[c] = "timestamp"
        elif "temp" in c_low:
            rename_map[c] = "temperature"
        elif c_low == "ph" or "ph_" in c_low or "ph " in c_low:
            rename_map[c] = "pH"
        elif "dissolved oxygen" in c_low or c_low in {"do", "do_mg_l", "do mg/l"}:
            rename_map[c] = "dissolved_oxygen"

    df = df.rename(columns=rename_map)

    required = ["timestamp", "temperature", "pH", "dissolved_oxygen"]
    missing = [c for c in required if c not in df.columns]
    if missing:
        raise ValueError(
            f"Missing expected columns after renaming: {missing}\n"
            f"Available columns are: {list(df.columns)}"
        )

    out = df[required].copy()
    out["timestamp"] = pd.to_datetime(out["timestamp"], errors="coerce")
    out = out.dropna(subset=["timestamp"]).sort_values("timestamp").reset_index(drop=True)
    return out


def main() -> None:
    path = Path(RAW_FILE)
    if not path.exists():
        raise FileNotFoundError(
            f"Could not find {RAW_FILE}. Download an EPA raw-data ZIP, extract it, and point RAW_FILE to the correct CSV/XLSX file."
        )

    if path.suffix.lower() == ".csv":
        df = pd.read_csv(path)
    elif path.suffix.lower() in {".xlsx", ".xls"}:
        df = pd.read_excel(path)
    else:
        raise ValueError("RAW_FILE must be CSV or Excel.")

    clean = standardize_columns(df)
    clean.to_csv(OUTPUT_FILE, index=False)
    print(f"Saved standardized file: {OUTPUT_FILE}")
    print(clean.head())


if __name__ == "__main__":
    main()